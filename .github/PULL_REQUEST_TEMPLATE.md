<!--
One concern per change. Daniel merges, never the author of the change.

The five headings below are the shape. What, Why and Verified are always
answered. Open questions and Follow-ups are deleted when there are none - an
empty heading reads as "considered and found empty".

Language: English - this is project steering. Quoted vault content, note names
and coined terms stay German.
-->

## What

<!-- What this change does, concretely. -->

## Why

<!-- The problem it solves. If it follows from a decision record or a ticket,
     say which, and link it. -->

## Verified

<!-- How you know it works. Name the commands you ran and what they returned -
     not what they would have returned. State what you did NOT verify.

     For this project that always includes: every written file was read back in
     full (never `tail`), null bytes counted, and each `str.replace()` confirmed
     to have found its pattern. If `method.json` or anything under `10 Method/`
     changed, paste what `check-method.mjs` actually printed, including the
     number of references it read and what it says it could not check. -->

## Open questions

<!-- Numbered O1..On, each with a recommended default. Delete if none.

     Do not answer them yourself. When the answer comes it arrives as a comment
     on this pull request, naming the O-number - and nothing already written
     here is rewritten. This section keeps one line per question:
     `O1 - answered: … → <link>`.

     An answer edited into this description overwrites the question it answers.
     It gets no permalink, notifies nobody, carries no timestamp except one
     typed by hand, and races whoever else is editing. The durable consequence
     goes where it belongs: a decision record, or `10 Method/STATUS.md`. -->

## Follow-ups

<!-- What this deliberately leaves undone, and where it is recorded. -->
